---
phase: 04-data-lineage-field-tracing-and-stage-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DATA_LINEAGE.md
autonomous: true
requirements:
  - LIN-01

must_haves:
  truths:
    - "DATA_LINEAGE.md exists in the MDPA repo root with the full document skeleton"
    - "Part 1 contains entries for all 4 source systems plus the Reference/Supplementary sources with confirmed XML field names"
    - "Each source system entry lists its actual XML field names (not doc 6 conceptual names)"
    - "The intermediate staging file paths (LoanFileTmp.yxdb, ChargeOffTmp.yxdb, etc.) are documented as the two-step input path"
  artifacts:
    - path: "DATA_LINEAGE.md"
      provides: "Lineage map skeleton with Part 1 fully populated"
      min_lines: 80
      contains: "Source 1: Loan Portfolio"
  key_links:
    - from: "Source 1/2 (CU-uploaded files)"
      to: "LoanFileTmp.yxdb / ChargeOffTmp.yxdb (staging)"
      via: "DynamicInput JSON routing"
      pattern: "DynamicInput.*LoanFileTmp"
---

<objective>
Create DATA_LINEAGE.md with the full document skeleton (all 5 parts, all section headers) and populate Part 1 with the complete source system inventory extracted from the XML.

Purpose: Establish the foundation of the lineage map. All downstream plans write into this file. Part 1 documents the starting point of every lineage trace — where each field originates before entering the workflow.

Output: DATA_LINEAGE.md with skeleton + fully populated Part 1 (Source Systems section).
</objective>

<execution_context>
@/home/mabushanab/.claude/get-shit-done/workflows/execute-plan.md
@/home/mabushanab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-lineage-field-tracing-and-stage-mapping/04-RESEARCH.md
@.planning/phases/04-data-lineage-field-tracing-and-stage-mapping/04-VALIDATION.md

<interfaces>
<!-- Key confirmed field names from XML (Phase 1 + Phase 4 research) — use these verbatim -->

Source 1 — Loan Portfolio (via DynamicInput from CU-uploaded files):
  `Loan Type`, `Loan Group`, `Loan Subgroup`, `Allowance Group`, `LoanAllowanceGroup`
  `Report Date`, `PeerNo`, `Origination Date`, `Charge Off Date`
  `Interest Rate`, `Original Credit Score`, `Term`, `Model Year`
  `Loan Description`, `PeerGroupName`

Source 2 — Charge-Off/Recovery (via DynamicInput from CU-uploaded files):
  `Charge Off Amount`, `Charge Off Date`, `Recovery Amount`, `Recovery Date`
  `Charge Offs` (distinct from `Charge Off Amount` — used in Net Charge Off conditional)

Source 3 — Real Estate Valuations (via Append RE Values.yxmc macro):
  Specific fields not confirmed from FormulaField scan — document as "fields appended by
  Append RE Values.yxmc macro; individual field names to be confirmed via macro XML in Phase 6"

Source 4 — TransUnion Credit Bureau (via TransUnion Mask_FICO Only_v2 macro):
  `Original Credit Score` (confirmed — used in Decision FICO Grade formula)
  Additional fields masked by macro — document as "additional fields masked by
  TransUnion Mask_FICO Only_v2.yxmc; specifics in macro XML (Phase 6)"

Reference/Supplementary Sources (TTA internal files — confirmed from XML):
  - `0000_19001231_SECURITIES.yxdb` — securities data
  - `CallReportDataShort.yxdb` — provides `Probability of Default`, `Total Assets`, `Net Worth`, `ALLL`
    (XML annotation: "Adding 3 fields from the call report data each quarter")
  - `0000_20170125_PROBABILITY OF DEFAULT.xlsx` — PD lookup table
  - `0000_MASTER_PARTICIPATIONS.yxdb` — participation loans master
  - `NAICS PD_20190731.xlsx` — NAICS-based PD reference
  - `CO Data SBA - Excel Version.xlsx` — SBA charge-off reference data
  - `0000_19000101_CURRENT RE MODEL.xlsx` — real estate model parameters
  - `Zip Code Ethnicity Index.csv` — Fair Lending demographic lookup

Intermediate staging files (two-step input path confirmed from XML):
  \\10.2.7.56\Shared\PortfolioAnalysis\99_References\LoanFileTmp.yxdb
  \\10.2.7.56\Shared\PortfolioAnalysis\99_References\ImpairedLoanTmp.yxdb
  \\10.2.7.56\Shared\PortfolioAnalysis\99_References\ChargeOffTmp.yxdb
  \\10.2.7.56\Shared\PortfolioAnalysis\99_References\SecuritiesTmp.yxdb
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DATA_LINEAGE.md skeleton and populate Part 1 (Source Systems)</name>
  <files>DATA_LINEAGE.md</files>
  <action>
    Create `/home/mabushanab/claude-agents/MDPA/DATA_LINEAGE.md` with the full document skeleton and populate Part 1 completely.

    **Ground truth rule:** XML file is authoritative. Do not use doc 6 field names without XML confirmation. Field names above are confirmed from XML extraction during Phase 1 and Phase 4 research.

    **Document skeleton — create ALL 5 parts with section headers (Parts 2-5 left as stubs to be populated in Plans 02 and 03):**

    ```
    # MDPA Data Lineage Map

    **Generated:** [today's date]
    **Workflow:** 2020_DataProcess_v5.2.yxmd (49,082 lines XML)
    **Ground truth:** XML file is authoritative. Doc field names and formulas verified against XML.
    **Phase 1 corrections applied:**
      - `Risk_Score` does not exist in XML — correct field is `Decision FICO Grade` (categorical A+/A/B/C/D/E)
      - `Net Charge Off Amount` uses conditional formula, not simple subtraction
      - 7-stage model (XML ToolContainer structure), not 5-stage model from doc 6
      - Vintage Adjustment values are pre-computed and carried in from prior period, not recalculated

    ## Part 1: Source Systems and Input Fields
    ## Part 2: Processing Stage Transformations   ← stub header only
    ## Part 3: Calculated/Derived Field Formulas  ← stub header only
    ## Part 4: Output Field Mapping               ← stub header only
    ## Part 5: End-to-End Traceability Examples   ← stub header only
    ```

    **Populate Part 1 completely:**

    Structure Part 1 with these subsections:

    ### 1.1 Source 1: Loan Portfolio (CU-Uploaded Files via DynamicInput)
    - Describe the two-step ingestion path: JSON_Input → JSONParse → RegEx/Filter → DynamicInput → LoanFileTmp.yxdb → Stage 1 processing
    - List all confirmed input fields in a Markdown table with columns: Field Name | Type/Notes | Used In
    - Use the confirmed XML field names listed in the interfaces block above
    - Note: DynamicInput routing uses `FileGroupNum` from JSON metadata to route to institution-specific files

    ### 1.2 Source 2: Charge-Off/Recovery (CU-Uploaded Files via DynamicInput)
    - Same two-step staging path via ChargeOffTmp.yxdb
    - List confirmed fields: `Charge Off Amount`, `Charge Off Date`, `Recovery Amount`, `Recovery Date`, `Charge Offs`
    - Note the distinction between `Charge Offs` and `Charge Off Amount` (different fields, both present)

    ### 1.3 Source 3: Real Estate Valuations (via Append RE Values.yxmc)
    - Document the macro as the ingestion mechanism — individual fields not confirmed from FormulaField scan
    - Note: appends real estate valuation fields to loan records; specifics depend on macro XML (Phase 6)
    - Cross-reference macro path: `Append RE Values.yxmc`

    ### 1.4 Source 4: TransUnion Credit Bureau (via TransUnion Mask_FICO Only_v2.yxmc)
    - Document `Original Credit Score` as the confirmed output field used in Decision FICO Grade formula
    - Note: macro masks additional TransUnion fields; specifics in macro XML (Phase 6)
    - Cross-reference macro path: `TransUnion Mask_FICO Only_v2.yxmc`

    ### 1.5 Reference/Supplementary Sources (TTA Internal Files)
    - Table listing all 8 supplementary input files confirmed from XML (see interfaces block)
    - Include columns: File | Format | Fields Provided | Usage in Workflow
    - For CallReportDataShort.yxdb, note the 3 confirmed fields: `Probability of Default`, `Total Assets`, `Net Worth`, `ALLL`

    ### 1.6 Input Staging Architecture
    - Diagram (ASCII or prose) showing the two-step input path:
      CU-uploaded files → DynamicInput routing → *.yxdb staging files → Stage 1
    - Show all 4 staging file paths with their UNC paths

    Before writing, run these XML extraction commands to verify field names and confirm no additional source fields are missing:
    ```bash
    # Confirm loan portfolio field names used in formulas
    grep -oP 'FormulaField expression="[^"]+" field="[^"]+"' \
      /home/mabushanab/claude-agents/MDPA/2020_DataProcess_v5.2.yxmd | head -60

    # Confirm input file paths
    grep -oP '\\\\[^<"]+\.(yxdb|xlsx|csv)' \
      /home/mabushanab/claude-agents/MDPA/2020_DataProcess_v5.2.yxmd | sort -u

    # Check for LTV/Delinquency fields (expected not found — confirm absence)
    grep -in 'LTV\|Delinquency\|Charge_Off_Rate\|Risk_Score' \
      /home/mabushanab/claude-agents/MDPA/2020_DataProcess_v5.2.yxmd | head -20
    ```

    Document the LTV/Delinquency finding: if these fields are not found, note "Not confirmed in XML FormulaField scan; may be inside macro logic (Phase 6) or computed as Summarize aggregations. Flagged as open question."
  </action>
  <verify>
    <automated>grep -c "^### " /home/mabushanab/claude-agents/MDPA/DATA_LINEAGE.md</automated>
  </verify>
  <done>
    DATA_LINEAGE.md exists with 5 top-level Part sections and all 1.x subsections fully populated.
    All 4 source systems documented with confirmed XML field names.
    All 8 supplementary source files listed.
    Two-step staging architecture described.
    LTV/Delinquency absence noted explicitly.
    Parts 2-5 present as stub headers (populated by later plans).
  </done>
</task>

</tasks>

<verification>
```bash
# File exists
ls -la /home/mabushanab/claude-agents/MDPA/DATA_LINEAGE.md

# All 5 parts present
grep -c "^## Part" /home/mabushanab/claude-agents/MDPA/DATA_LINEAGE.md

# Source systems documented
grep -c "Source [1234]" /home/mabushanab/claude-agents/MDPA/DATA_LINEAGE.md

# Key source fields confirmed
grep "Original Credit Score\|Charge Off Amount\|Loan Type\|PeerNo" \
  /home/mabushanab/claude-agents/MDPA/DATA_LINEAGE.md
```
</verification>

<success_criteria>
DATA_LINEAGE.md exists with full document skeleton (5 parts) and Part 1 completely populated with:
- 4 source system entries (Loan Portfolio, Charge-Off/Recovery, RE Valuations, TransUnion)
- All Reference/Supplementary sources listed (8 files)
- Two-step staging architecture documented
- LTV/Delinquency absence noted
- XML-confirmed field names used throughout (no doc 6 conceptual names)
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-lineage-field-tracing-and-stage-mapping/04-01-SUMMARY.md`
</output>
