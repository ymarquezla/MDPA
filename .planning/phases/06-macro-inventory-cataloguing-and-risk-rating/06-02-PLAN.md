---
phase: 06-macro-inventory-cataloguing-and-risk-rating
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - MACRO_INVENTORY.md
autonomous: false
requirements:
  - MAC-03
  - MAC-04
must_haves:
  truths:
    - "MACRO_INVENTORY.md contains a Deployment Risk Register section mapping all 20 macros to their risk tier with remediation references"
    - "MACRO_INVENTORY.md contains a Macro Dependency Map section showing the 9-stage execution order with macros grouped by stage"
    - "Every macro in the risk register is mapped to exactly one of three risk tiers (A/B/C) with the correct GAP reference"
    - "The dependency map clearly distinguishes active macros from disabled macros"
    - "A human reviewer has confirmed the document is complete, accurate, and usable without opening the Alteryx XML"
  artifacts:
    - path: "MACRO_INVENTORY.md"
      provides: "Complete macro inventory including deployment risk register and dependency map — all four MAC requirements satisfied"
      contains: "Deployment Risk Register"
      min_lines: 500
  key_links:
    - from: "MACRO_INVENTORY.md Deployment Risk Register"
      to: "GAP_ANALYSIS.md G02-001 through G02-017"
      via: "remediation cross-reference (REM-001, REM-002, REM-003)"
      pattern: "REM-00"
    - from: "MACRO_INVENTORY.md Macro Dependency Map"
      to: "06-RESEARCH.md Stage 1 through Stage 9"
      via: "execution stage groupings from XML line-number ordering"
      pattern: "Stage [0-9]"
---

<objective>
Complete MACRO_INVENTORY.md by populating the Deployment Risk Register (MAC-03) and Macro Dependency Map (MAC-04) sections, then obtain human sign-off that the document is complete and usable.

Purpose: Satisfy MAC-03 (deployment risk rating per macro, actionable by an engineer) and MAC-04 (full dependency map showing execution order), completing all four Phase 6 requirements before Phase 7 Confluence publication.
Output: MACRO_INVENTORY.md fully populated — all five sections complete. Human-approved document ready for Phase 7 publication.
</objective>

<execution_context>
@/home/mabushanab/.claude/get-shit-done/workflows/execute-plan.md
@/home/mabushanab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GAP_ANALYSIS.md
@.planning/phases/06-macro-inventory-cataloguing-and-risk-rating/06-RESEARCH.md
@.planning/phases/06-macro-inventory-cataloguing-and-risk-rating/06-01-SUMMARY.md

<interfaces>
<!-- Plan 01 created MACRO_INVENTORY.md with sections 1-3 populated and stubs for sections 4-5. -->
<!-- This plan populates sections 4 (Deployment Risk Register) and 5 (Macro Dependency Map). -->

DEPLOYMENT RISK TIERS (from GAP_ANALYSIS.md G02-001 through G02-017):

Tier A — Temp-Path Embedded (15 macros):
Path pattern: D:\Users\vnekkanti\AppData\Local\Temp\1\Staging\a6b96bdf-...\Macros\
All 15 fail on any machine other than the original developer's workstation.
Remediation: REM-001 — Relocate all 15 macros to shared UNC path \\10.2.7.56\Shared\Prod\Macros\MDPA\

Macros in Tier A (in dependency/execution order):
| Macro | GAP Ref | Instances | Active/Disabled |
|-------|---------|-----------|-----------------|
| Union Subset Prior Period.yxmc | G02-001 | 1 | Active |
| Generate Unique ID.yxmc | G02-002 | 1 | Active |
| Dropped Records Prep.yxmc | G02-003 | 1 | Active |
| Last Name Comma First Name Cleaner_v2.yxmc | G02-004 | 1 | Active |
| Preliminary Client File Match.yxmc | G02-005 | 1 | Active |
| 2020_Date_Converter.yxmc | G02-006 | 5 | Active |
| Append Charge Offs and Matching.yxmc | G02-007 | 1 | Active |
| Append RE Values.yxmc | G02-008 | 1 | Active |
| Auto Value Append.yxmc | G02-009 | 1 | Active |
| TransUnion Mask_FICO Only_v2.yxmc | G02-010 | 1 | Active |
| 2020_Publish2Server.yxmc | G02-011 | 1 | DISABLED |
| 2020_PublishDropped2Server.yxmc | G02-012 | 1 | DISABLED |
| PreProcess_Iterative.yxmc | G02-013 | 1 | Active |
| Only Prior Period.yxmc | G02-014 | 1 | Active |
| Contingent File Input.yxmc | G02-015 | 8 | Active |

Tier B — External Add-On / _externals\1\ path (1 macro):
| Macro | GAP Ref | Instances | Active/Disabled |
|-------|---------|-----------|-----------------|
| 2020_PublishSecurities2Server.yxmc | G02-016 | 1 | DISABLED |
Note: Also DISABLED as of 2026-03-18. Source .yxmc file location unknown (see REM-002).
Remediation: REM-002 — Locate source .yxmc file; if found, relocate to shared UNC path.

Tier C — External Library / No path prefix (7 macros, 4 CReW + 3 Tableau):
All resolved from Alteryx Server macro search path — require library installation.
Remediation: REM-003 — Install CReW Runner library on Alteryx Server; verify Tableau connector installed.

| Macro | Library | GAP Ref | Instances | Active/Disabled |
|-------|---------|---------|-----------|-----------------|
| CReW_EnsureFields.yxmc | CReW | G02-017 | 8 | Active |
| CReW_ParallelBlockUntilDone.yxmc | CReW | G02-017 | 1 | Active |
| Cleanse.yxmc | CReW | G02-017 | 2 | Active |
| Ethnic & Gender ID.yxmc | CReW | G02-017 | 1 | Active |
| Tableau New Macro.yxmc | Tableau | G02-017 | 1 | Active |
| Tableau New Macro Dropped.yxmc | Tableau | G02-017 | 1 | Active |
| Tableau New Macro Securities.yxmc | Tableau | G02-017 | 1 | Active |

REMEDIATION REFERENCES (from GAP_ANALYSIS.md):
- REM-001: "Relocate all 15 embedded macros from temp path to shared UNC path \\10.2.7.56\Shared\Prod\Macros\MDPA\ and update all 15 EngineSettings Macro= references in the .yxmd XML to point to the new UNC path."
- REM-002: "Locate the source .yxmc file for 2020_PublishSecurities2Server.yxmc (check original developer's workstation or source control). Relocate to shared UNC path. Note: also currently DISABLED — remediate path before re-enabling if TDE format support is restored."
- REM-003: "Install and register CReW Runner macro library on the Alteryx Server execution host. Verify Tableau Connector SDK is installed. Confirm macro search path includes the CReW library directory."

EXECUTION STAGE MAP (from 06-RESEARCH.md Architecture Patterns):
Stage 1 — Input Loading (XML lines ~46,000+): Contingent File Input.yxmc (x8)
Stage 2 — Data Union / Prior Period (XML lines ~319): Union Subset Prior Period.yxmc (x1)
Stage 3 — Preprocessing (XML lines ~43,550): PreProcess_Iterative.yxmc (x1)
Stage 4 — Validation Gates (XML lines ~4,633; 38,092–41,528): CReW_EnsureFields.yxmc (x8)
Stage 5 — Data Transformation (XML lines ~36,560–40,793, multiple tools):
  Generate Unique ID.yxmc (x1)
  Dropped Records Prep.yxmc (x1)
  Last Name Comma First Name Cleaner_v2.yxmc (x1)
  Cleanse.yxmc (x2)
  Preliminary Client File Match.yxmc (x1)
  2020_Date_Converter.yxmc (x5)
  Append Charge Offs and Matching.yxmc (x1)
  Ethnic & Gender ID.yxmc (x1)
  TransUnion Mask_FICO Only_v2.yxmc (x1)
  Append RE Values.yxmc (x1)
  Auto Value Append.yxmc (x1)
Stage 6 — Period Filtering (XML lines ~43,978): Only Prior Period.yxmc (x1)
Stage 7 — Flow Control (XML lines ~16,923): CReW_ParallelBlockUntilDone.yxmc (x1)
Stage 8 — Output Formatting / Active Publishing (XML lines ~29,339):
  Tableau New Macro.yxmc (x1)
  Tableau New Macro Dropped.yxmc (x1)
  Tableau New Macro Securities.yxmc (x1)
Stage 9 — Server Publishing / DISABLED (XML lines ~47,237–47,321, Container 1049):
  2020_Publish2Server.yxmc (x1) — DISABLED
  2020_PublishDropped2Server.yxmc (x1) — DISABLED
  2020_PublishSecurities2Server.yxmc (x1) — DISABLED

IMPORTANT NOTE ON STAGE ORDERING: XML line numbers reflect declaration order, not guaranteed Alteryx
execution order (Alteryx uses a parallel processing engine). Stage groupings are reliable; intra-stage
ordering within Stage 5 is approximate. Document as "approximate execution stage" not "strict serial order."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate Deployment Risk Register section (MAC-03)</name>
  <files>/home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md</files>
  <action>
Replace the stub content under `## Deployment Risk Register` in MACRO_INVENTORY.md with the full risk register.

The section should contain:

**Opening paragraph:**
"All 20 unique macros in the MDPA v5.2 workflow carry CRITICAL deployment risk. The three risk tiers below map each macro to its failure mode, the GAP finding that identified it, and the remediation action required. All remediation actions are defined in GAP_ANALYSIS.md."

**Risk Register Table — Tier A (15 macros):**

Heading: `### Tier A — Temp-Path Embedded (15 macros)`

Description paragraph: "Path pattern: `D:\Users\vnekkanti\AppData\Local\Temp\1\Staging\a6b96bdf-...\Macros\`. These macros were embedded in the workflow and extracted to the original developer's machine-specific temp directory when the workflow was opened. The path does not exist on any other machine. Remediation: **REM-001** — Relocate all 15 macros to shared UNC path `\\10.2.7.56\Shared\Prod\Macros\MDPA\` and update all 15 `EngineSettings Macro=` references in the .yxmd XML."

Table:
| Macro | GAP Finding | Instances | Status | Required Action |
|-------|------------|-----------|--------|-----------------|
| Contingent File Input.yxmc | G02-015 | 8 | Active | REM-001: Relocate to UNC path |
| Union Subset Prior Period.yxmc | G02-001 | 1 | Active | REM-001: Relocate to UNC path |
| PreProcess_Iterative.yxmc | G02-013 | 1 | Active | REM-001: Relocate to UNC path |
| Generate Unique ID.yxmc | G02-002 | 1 | Active | REM-001: Relocate to UNC path |
| Dropped Records Prep.yxmc | G02-003 | 1 | Active | REM-001: Relocate to UNC path |
| Last Name Comma First Name Cleaner_v2.yxmc | G02-004 | 1 | Active | REM-001: Relocate to UNC path |
| Preliminary Client File Match.yxmc | G02-005 | 1 | Active | REM-001: Relocate to UNC path |
| 2020_Date_Converter.yxmc | G02-006 | 5 | Active | REM-001: Relocate to UNC path |
| Append Charge Offs and Matching.yxmc | G02-007 | 1 | Active | REM-001: Relocate to UNC path |
| Append RE Values.yxmc | G02-008 | 1 | Active | REM-001: Relocate to UNC path |
| Auto Value Append.yxmc | G02-009 | 1 | Active | REM-001: Relocate to UNC path |
| TransUnion Mask_FICO Only_v2.yxmc | G02-010 | 1 | Active | REM-001: Relocate to UNC path |
| Only Prior Period.yxmc | G02-014 | 1 | Active | REM-001: Relocate to UNC path |
| 2020_Publish2Server.yxmc | G02-011 | 1 | DISABLED | REM-001 (if re-enabled); also requires TDE format support |
| 2020_PublishDropped2Server.yxmc | G02-012 | 1 | DISABLED | REM-001 (if re-enabled); also requires TDE format support |

**Risk Register Table — Tier B (1 macro):**

Heading: `### Tier B — External Add-On / Non-Standard Path (1 macro)`

Description paragraph: "Path pattern: `D:\Users\vnekkanti\AppData\Local\Temp\1\Staging\a6b96bdf-..._externals\1\`. This macro was packaged as an external add-on (not embedded with the standard macro set) and extracted to a non-standard `_externals\1\` subdirectory. The source .yxmc file location is unknown — it was not committed to a shared location and was discovered only via XML inspection (GAP G01-009). Remediation: **REM-002** — Locate the source .yxmc file on the original developer's workstation or in source control; relocate to shared UNC path."

Table:
| Macro | GAP Finding | Instances | Status | Required Action |
|-------|------------|-----------|--------|-----------------|
| 2020_PublishSecurities2Server.yxmc | G02-016 | 1 | DISABLED | REM-002: Locate source file; source .yxmc location unknown |

**Risk Register Table — Tier C (7 macros):**

Heading: `### Tier C — External Library / No Path Prefix (7 macros)`

Description paragraph: "No path prefix in XML — these macros are resolved from the Alteryx Server macro search path at runtime. They require external software libraries to be installed and registered: CReW Runner (for 4 CReW macros) and Tableau Connector (for 3 Tableau macros). If either library is absent, all dependent macro instances fail at runtime with no fallback. Remediation: **REM-003** — Install CReW Runner library on Alteryx Server; verify Tableau Connector SDK is installed; confirm macro search path includes both library directories."

Table:
| Macro | Library | GAP Finding | Instances | Status | Required Action |
|-------|---------|------------|-----------|--------|-----------------|
| CReW_EnsureFields.yxmc | CReW Runner | G02-017 | 8 | Active | REM-003: Install CReW Runner |
| CReW_ParallelBlockUntilDone.yxmc | CReW Runner | G02-017 | 1 | Active | REM-003: Install CReW Runner |
| Cleanse.yxmc | CReW Runner | G02-017 | 2 | Active | REM-003: Install CReW Runner |
| Ethnic & Gender ID.yxmc | CReW Runner | G02-017 | 1 | Active | REM-003: Install CReW Runner + Zip Code Ethnicity Index CSV (G02-018) |
| Tableau New Macro.yxmc | Tableau Connector | G02-017 | 1 | Active | REM-003: Install Tableau Connector; configure DCM PAT connection |
| Tableau New Macro Dropped.yxmc | Tableau Connector | G02-017 | 1 | Active | REM-003: Install Tableau Connector; configure DCM PAT connection |
| Tableau New Macro Securities.yxmc | Tableau Connector | G02-017 | 1 | Active | REM-003: Install Tableau Connector; configure DCM PAT connection |

**Remediation Summary box** (after all three tier tables):
```
**Remediation Summary:**
- REM-001: Update 15 macro paths in the .yxmd XML to UNC path \\10.2.7.56\Shared\Prod\Macros\MDPA\
- REM-002: Locate 2020_PublishSecurities2Server.yxmc source file; relocate to UNC path
- REM-003: Install CReW Runner + Tableau Connector on execution server; configure DCM PAT connection
See GAP_ANALYSIS.md Remediation List for full action steps.
```
  </action>
  <verify>
    <automated>grep -c "REM-00" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md</automated>
  </verify>
  <done>
`grep -c "REM-00" MACRO_INVENTORY.md` returns at least 20 (one per risk register table row plus summary). The Deployment Risk Register section contains three tier subsections (Tier A, Tier B, Tier C). All 20 unique macros appear in exactly one tier. The remediation summary paragraph lists REM-001, REM-002, and REM-003.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate Macro Dependency Map section (MAC-04)</name>
  <files>/home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md</files>
  <action>
Replace the stub content under `## Macro Dependency Map` in MACRO_INVENTORY.md with the full dependency map.

The section should contain:

**Opening paragraph:**
"The dependency map groups macros by their approximate execution stage, derived from XML declaration line numbers in the 49,082-line workflow file. Stage ordering is reliable; intra-stage ordering within Stage 5 (Data Transformation) is approximate — Alteryx's parallel processing engine does not guarantee strict serial execution within a stage. All execution line numbers are from the 2020_DataProcess_v5.2.yxmd XML file, verified 2026-03-19."

**Visual dependency diagram** using code block:

```
MDPA v5.2 Workflow — Macro Execution Map
========================================

Stage 1 — Input Loading (XML lines ~46,000+)
  └── Contingent File Input.yxmc [x8] — 8 parallel client file groups

Stage 2 — Data Union / Prior Period (XML lines ~319)
  └── Union Subset Prior Period.yxmc [x1] — merges current + prior period records

Stage 3 — Preprocessing (XML lines ~43,550)
  └── PreProcess_Iterative.yxmc [x1] — iterative field standardization

Stage 4 — Validation Gates (XML lines ~4,633; ~38,092–41,528)
  └── CReW_EnsureFields.yxmc [x8] — 8 field validation checkpoints

Stage 5 — Data Transformation (XML lines ~36,560–40,793) [approximate intra-stage order]
  ├── Generate Unique ID.yxmc [x1]
  ├── Dropped Records Prep.yxmc [x1]
  ├── Last Name Comma First Name Cleaner_v2.yxmc [x1]
  ├── Cleanse.yxmc [x2]
  ├── Preliminary Client File Match.yxmc [x1]
  ├── 2020_Date_Converter.yxmc [x5]
  ├── Append Charge Offs and Matching.yxmc [x1]
  ├── Ethnic & Gender ID.yxmc [x1]
  ├── TransUnion Mask_FICO Only_v2.yxmc [x1]
  ├── Append RE Values.yxmc [x1]
  └── Auto Value Append.yxmc [x1]

Stage 6 — Period Filtering (XML lines ~43,978)
  └── Only Prior Period.yxmc [x1] — extracts prior-period subset for comparison

Stage 7 — Flow Control (XML lines ~16,923)
  └── CReW_ParallelBlockUntilDone.yxmc [x1] — synchronizes parallel branches

Stage 8 — Output Formatting / Active Publishing (XML lines ~29,339)
  ├── Tableau New Macro.yxmc [x1] — main client file → Hyper → Tableau Server
  ├── Tableau New Macro Dropped.yxmc [x1] — dropped records → Hyper → Tableau Server
  └── Tableau New Macro Securities.yxmc [x1] — securities data → Hyper → Tableau Server

Stage 9 — Legacy Publishing [DISABLED] (Container 1049, XML lines ~47,237–47,321)
  ├── 2020_Publish2Server.yxmc [x1] — DISABLED (TDE format, superseded by Stage 8)
  ├── 2020_PublishDropped2Server.yxmc [x1] — DISABLED (TDE format, superseded by Stage 8)
  └── 2020_PublishSecurities2Server.yxmc [x1] — DISABLED (TDE format, _externals\1\ path)
```

**Stage-to-Output Flow table:**

Heading: `### Stage-to-Output Mapping`

| Output File | Producing Stages | Key Macros in Chain |
|-------------|-----------------|---------------------|
| Client File (.yxdb) | Stages 1–7 | Contingent File Input → Validate → Transform → Tableau New Macro |
| Dropped Records (.yxdb) | Stages 1–5 (rejected) | Contingent File Input → Preliminary Client File Match (reject) → Dropped Records Prep → Tableau New Macro Dropped |
| Securities Extract | Stages 1–5, 8 | Contingent File Input → Append RE Values → Auto Value Append → Tableau New Macro Securities |
| QA Report | Stages 1–7 | Full pipeline summary — feeds PortfolioComposerTable (non-macro tool) |
| Executive Summary | Stages 1–7 | Full pipeline — feeds PortfolioEmail (non-macro tool) |

**Key dependency notes:**

Heading: `### Key Dependency Notes`

Bulleted list:
- Stage 1 (Contingent File Input x8) must complete before Stage 2 — all 8 client file groups must be loaded before union.
- Stage 4 (CReW_EnsureFields x8) gates entry to Stage 5 — transformation macros in Stage 5 receive only validated records.
- Stage 7 (CReW_ParallelBlockUntilDone) synchronizes Stage 5 parallel branches before Stage 8 publishing begins.
- Stage 8 and Stage 9 are mutually exclusive — Stage 9 (Container 1049) is DISABLED. Stage 8 is the active publishing path.
- Stage 5 contains 11 distinct macro types running as parallel sub-streams — intra-stage ordering is approximate.
- The three CReW validation macros (Stages 4 and 7) require CReW Runner library (Tier C risk). If CReW Runner is absent, the workflow halts at Stage 4 — no output is produced.
  </action>
  <verify>
    <automated>grep -q "Stage 1" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md && grep -q "Stage 9" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md && echo "PASS"</automated>
  </verify>
  <done>
Both Stage 1 and Stage 9 references exist in MACRO_INVENTORY.md. The dependency map code block is present. The Stage-to-Output Mapping table exists. Key Dependency Notes section contains at least 5 bullet points. The DISABLED stage (Stage 9) is visually distinct from active stages.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review — confirm MACRO_INVENTORY.md is complete and accurate</name>
  <files>/home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md</files>
  <what-built>
MACRO_INVENTORY.md — the complete Phase 6 macro catalogue for MDPA v5.2. Contains:
- Executive Summary with category and risk-tier count tables (20 unique macros, 41 instances)
- 23 numbered macro entries (20 unique files + 3 disabled publish macros as separate entries) each with property table, purpose, inputs, outputs, logic summary, and deployment notes
- Deployment Risk Register covering all three tiers (A/B/C) with GAP references and remediation actions
- Macro Dependency Map with 9-stage execution visualization and stage-to-output mapping

All four MAC requirements (MAC-01 through MAC-04) are addressed in this document.
  </what-built>
  <action>Run the automated verification commands below, then open MACRO_INVENTORY.md to review at least 3 macro entries, the Deployment Risk Register, and the Macro Dependency Map. Confirm that all MAC requirements (MAC-01 through MAC-04) are satisfied.</action>
  <how-to-verify>
Run these checks first:
```bash
# Should return 23 (20 unique + 3 disabled publish macros as entries 21-23)
grep -c "^### " /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md

# Should return 20+ (one per macro entry)
grep -c "Logic Summary" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md

# Should return 20+ (one per risk register row)
grep -c "REM-00" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md

# Should print PASS
grep -q "Stage 1" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md && grep -q "Stage 9" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md && echo PASS
```

Then open MACRO_INVENTORY.md and verify:
1. Header states "20 unique macro files, 41 instances" (not "23 unique")
2. Executive Summary tables are present with correct category/instance counts that sum to 41
3. Pick any 3 macro entries — confirm each has: property table, Purpose, Inputs, Outputs, Logic Summary, Deployment Notes
4. Disabled macros (entries 21-23 / 2020_Publish*.yxmc) clearly show "DISABLED" in their Status row
5. CReW macro entries note "Internal logic is inferred from CReW community documentation"
6. Deployment Risk Register has three tier sections (A/B/C) and all 20 macros appear in one tier
7. Macro Dependency Map shows all 9 stages with Stage 9 labeled DISABLED
8. An engineer reading this document would know exactly what to do to deploy the workflow on a new machine (REM-001, REM-002, REM-003)
  </how-to-verify>
  <resume-signal>
Type "approved" if MACRO_INVENTORY.md is complete and all four MAC requirements are satisfied. Describe any issues if corrections are needed — include the specific entry or section that needs fixing.
  </resume-signal>
  <verify>
    <automated>grep -c "^### " /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md</automated>
  </verify>
  <done>
All automated checks pass (23 entries, 20+ logic summaries, 20+ REM references, Stage 1 + Stage 9 present). Human reviewer has read at least 3 macro entries and the dependency map, confirmed the document is accurate and usable, and typed "approved."
  </done>
</task>

</tasks>

<verification>
Plan 06-02 is complete when ALL of the following are true:

1. `grep -c "^### " /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md` returns 23
2. `grep -c "Logic Summary" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md` returns at least 20
3. `grep -c "REM-00" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md` returns at least 20
4. `grep -q "Stage 1" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md && grep -q "Stage 9" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md && echo PASS` prints PASS
5. `grep -c "Tier A\|Tier B\|Tier C" /home/mabushanab/claude-agents/MDPA/MACRO_INVENTORY.md` returns at least 3
6. Human reviewer has typed "approved" in the Task 3 checkpoint
</verification>

<success_criteria>
1. MACRO_INVENTORY.md Deployment Risk Register maps all 20 unique macros to their risk tier with GAP reference and remediation action
2. MACRO_INVENTORY.md Macro Dependency Map shows all 9 execution stages with macros correctly grouped and Stage 9 DISABLED
3. An engineer reading only MACRO_INVENTORY.md can determine: (a) which macros need path relocation, (b) which require external library installation, and (c) which are disabled vs. active
4. Human reviewer confirms the document is complete, accurate, and satisfies all four MAC requirements
</success_criteria>

<output>
After completion, create `.planning/phases/06-macro-inventory-cataloguing-and-risk-rating/06-02-SUMMARY.md` with:
- Final line count of MACRO_INVENTORY.md
- All automated check results (grep counts)
- Human reviewer verdict and any issues found/fixed
- MAC-01, MAC-02, MAC-03, MAC-04 status: all complete
- Note: Phase 7 (Confluence publication) is the next phase — MACRO_INVENTORY.md is ready for publication
</output>
